[project]
name = "iati-project-location-standard"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = [
    "mkdocs-material>=9.5",
    "mkdocs-minify-plugin>=0.8",
]